---
phase: 01-safety-net-dead-code
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/scout/recipe.go
  - cmd/scout/runbook.go
  - cmd/scout/llm.go
  - internal/engine/fingerprint.go
  - pkg/scout/mcp/tools_websocket.go
  - pkg/scout/mcp/helpers.go
autonomous: true
requirements:
  - CLEAN-04
  - CLEAN-06
  - MCP-04
must_haves:
  truths:
    - "recipe.go is deleted from the codebase"
    - "No references to the recipe command remain in cmd/scout/"
    - "FingerprintToProfile is not exported from internal/engine/fingerprint.go"
    - "createProvider does not exist in cmd/scout/llm.go"
    - "tools_websocket.go has no truncate() function; it uses the one from helpers.go"
    - "go build ./... passes with no errors"
  artifacts:
    - path: "cmd/scout/runbook.go"
      provides: "Canonical applyVars and findUnresolvedVars helpers (no duplicates)"
    - path: "pkg/scout/mcp/tools_websocket.go"
      provides: "WebSocket MCP tools without a local truncate() definition"
    - path: "pkg/scout/mcp/helpers.go"
      provides: "Single canonical truncate() that respects maxLen"
  key_links:
    - from: "pkg/scout/mcp/tools_websocket.go"
      to: "pkg/scout/mcp/helpers.go"
      via: "truncate() call — same package, direct function call"
---

<objective>
Three cleanup tasks: (1) delete recipe.go entirely (past its 2026-04-15 deprecation date) and remove the duplicate applyVars/findUnresolvedVars helpers from runbook.go, (2) remove the dead exports FingerprintToProfile and createProvider, and (3) deduplicate the truncate() function by deleting the broken version in tools_websocket.go.

Purpose: recipe.go is a dead command file with functions that duplicate runbook.go. Keeping it creates confusion and carries two unused function bodies. FingerprintToProfile and createProvider are exports with zero live callers — the latter is even annotated nolint:unused. The duplicate truncate() in tools_websocket.go has a bug (can exceed maxLen); the version in helpers.go is correct and both are in the same package so they're immediately available to each other.

Output: recipe.go deleted, runbook.go with unique helpers only, llm.go without createProvider, fingerprint.go without FingerprintToProfile, tools_websocket.go without local truncate().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/code-quality.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete recipe.go and remove duplicate helpers from runbook.go</name>
  <files>
    cmd/scout/recipe.go
    cmd/scout/runbook.go
  </files>

  <read_first>
  - cmd/scout/recipe.go — read the full file to understand what commands it registers (so you can confirm none survive), and to find the exact names of applyVars and findUnresolvedVars
  - cmd/scout/runbook.go — read lines 530-580 to see applyRunbookVars and findUnresolvedRunbookVars (the canonical versions) and confirm they are functionally identical to the recipe.go versions
  - cmd/scout/scout.go — check for any AddCommand() call that references the recipe command root; if present, remove it
  </read_first>

  <action>
  **Step 1 — Delete recipe.go:**
  Delete the file `cmd/scout/recipe.go` entirely. Do not create a stub or redirect. Per D-08, the 2026-04-15 deprecation date has passed.

  **Step 2 — Remove duplicate helpers from runbook.go:**
  In cmd/scout/runbook.go, search for functions named `applyRunbookVars` and `findUnresolvedRunbookVars`. These are the canonical versions — keep them unchanged.
  If runbook.go also has any leftover wrapper that calls the now-deleted recipe.go functions, remove those wrappers.
  (The duplicate functions deleted were in recipe.go only: `applyVars` and `findUnresolvedVars`. The runbook.go versions have different names with the `Runbook` suffix and are already the right implementations.)

  **Step 3 — Remove recipe command registration:**
  In cmd/scout/scout.go (or wherever the root command AddCommand calls live), grep for any line adding the recipe command (e.g., `rootCmd.AddCommand(recipeCmd)` or similar). Remove it. If the variable `recipeCmd` was defined in recipe.go, it no longer exists — removing the AddCommand call is sufficient.

  **Step 4 — Grep verify no dangling references:**
  After deletion, scan cmd/scout/ for remaining references to `recipe`:
    - `recipeCmd` — must not appear
    - `applyVars(` — must not appear (the deleted function; `applyRunbookVars(` is fine to keep)
    - `findUnresolvedVars(` — must not appear (the deleted function)
  If any reference remains, remove it.

  Per D-10: breaking changes are acceptable. No stub command or redirect message needed.
  </action>

  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/... && go build ./...</automated>
  </verify>

  <acceptance_criteria>
  - cmd/scout/recipe.go does NOT exist on disk
  - cmd/scout/scout.go (or equivalent) does NOT contain `recipeCmd`
  - cmd/scout/ does NOT contain any file with `func applyVars(` (the deleted duplicate)
  - cmd/scout/ does NOT contain any file with `func findUnresolvedVars(` (the deleted duplicate)
  - cmd/scout/runbook.go contains `func applyRunbookVars(` (canonical version still present)
  - cmd/scout/runbook.go contains `func findUnresolvedRunbookVars(` (canonical version still present)
  - `go build ./cmd/scout/...` exits 0
  - `go build ./...` exits 0
  </acceptance_criteria>

  <done>recipe.go deleted. No duplicate applyVars/findUnresolvedVars remain. Build passes with no references to the deleted file.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead exports FingerprintToProfile and createProvider; fix truncate() duplicate</name>
  <files>
    internal/engine/fingerprint.go
    cmd/scout/llm.go
    pkg/scout/mcp/tools_websocket.go
  </files>

  <read_first>
  - internal/engine/fingerprint.go — read lines 35-60 to see the FingerprintToProfile function signature and body; confirm it is exported (starts with capital F)
  - cmd/scout/llm.go — read lines 600-640 to see the createProvider function; confirm the `//nolint:unused` annotation and that it has no callers in the file
  - pkg/scout/mcp/tools_websocket.go — search for `func truncate(` to find the local duplicate definition and understand what it does
  - pkg/scout/mcp/helpers.go — search for `func truncate(` to confirm the canonical version exists and read its signature: `func truncate(s string, maxLen int) string`
  </read_first>

  <action>
  **Change 1 — Delete FingerprintToProfile from internal/engine/fingerprint.go:**
  Find and delete the exported function `FingerprintToProfile`. This function's only caller is its own test file (`fingerprint_test.go`). Per D-11, test files can call unexported functions or be updated to test via other means — for now, simply delete the export. If the test file references `FingerprintToProfile`, also update that test to either remove the test or call an unexported version (rename the function to `fingerprintToProfile` — lowercase — and update the test accordingly). Do not break the test file.

  **Change 2 — Delete createProvider from cmd/scout/llm.go:**
  Find and delete the function `createProvider` (annotated `//nolint:unused`). Per D-12, this function has no callers. Remove the entire function body including its doc comment and the nolint annotation line. Do not remove any other functions from llm.go.

  **Change 3 — Delete duplicate truncate() from tools_websocket.go:**
  In pkg/scout/mcp/tools_websocket.go, find and delete the local `func truncate(` definition. The function in helpers.go (`func truncate(s string, maxLen int) string`) is in the same package (`package mcp`) and is immediately available — no import needed. Per D-14, the helpers.go version correctly respects maxLen; the tools_websocket.go version does not.
  After deletion, verify that any calls to `truncate(` in tools_websocket.go still resolve (they will, via helpers.go in the same package).
  </action>

  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./internal/engine/... && go build ./cmd/scout/... && go build ./pkg/scout/mcp/... && go test ./internal/engine/ -run TestFingerprint -v 2>&1 | head -20 && go build ./...</automated>
  </verify>

  <acceptance_criteria>
  - internal/engine/fingerprint.go does NOT contain `func FingerprintToProfile(` (export deleted)
  - cmd/scout/llm.go does NOT contain `func createProvider(` (dead function deleted)
  - cmd/scout/llm.go does NOT contain `nolint:unused` (the annotation removed with the function)
  - pkg/scout/mcp/tools_websocket.go does NOT contain `func truncate(` (local duplicate deleted)
  - pkg/scout/mcp/helpers.go contains `func truncate(` (canonical version present)
  - `go build ./...` exits 0
  - `go test ./internal/engine/` exits 0 (fingerprint tests still pass — either updated or still valid)
  </acceptance_criteria>

  <done>FingerprintToProfile deleted (or made unexported). createProvider deleted from llm.go. Local truncate() removed from tools_websocket.go. Only the canonical helpers.go truncate() remains in the mcp package. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `go build ./...` passes
- `cmd/scout/recipe.go` does not exist on disk
- grep for `panic(` in internal/engine/browser_rod.go, internal/engine/browser/manifest.go, pkg/scout/identity/identity.go returns no results (handled in plan 02 — verify not regressed)
- grep for `func truncate(` in pkg/scout/mcp/ returns exactly one result (helpers.go only)
- grep for `func createProvider(` in cmd/scout/ returns no results
- grep for `func FingerprintToProfile(` in internal/engine/ returns no results
</verification>

<success_criteria>
recipe.go is deleted. The duplicate applyVars/findUnresolvedVars helpers are gone (recipe.go deletion removes them). Dead exports FingerprintToProfile and createProvider are removed. tools_websocket.go uses the single canonical truncate() from helpers.go. Build passes. Phase 1 requirements CLEAN-04, CLEAN-06, and MCP-04 are fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-net-dead-code/01-03-SUMMARY.md`
</output>

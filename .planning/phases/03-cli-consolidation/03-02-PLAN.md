---
phase: 03-cli-consolidation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/scout/websearch.go
  - cmd/scout/search.go
  - cmd/scout/search_engines.go
  - cmd/scout/scout.go
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "`scout websearch` no longer appears in `scout --help` output"
    - "`scout search google` / `bing` / `duckduckgo` subcommands are removed"
    - "`scout search <query>` accepts `--engine` flag (google, bing, ddg, etc.) and optionally fetches result pages"
    - "`scout search wikipedia <query>` still works (unique engine kept)"
    - "`scout search --fetch` fetches the result pages (behaviour from old websearch)"
  artifacts:
    - path: cmd/scout/websearch.go
      provides: DELETED — file must not exist
    - path: cmd/scout/search_engines.go
      provides: DELETED — file must not exist
    - path: cmd/scout/search.go
      provides: Consolidated search command with --engine flag and --fetch capability
  key_links:
    - from: cmd/scout/search.go
      to: cmd/scout/scout.go
      via: rootCmd.AddCommand(searchCmd)
      pattern: "AddCommand.*search"
---

<objective>
Merge `websearch` into `search` and remove `search_engines.go` subcommands (CLI-03, per D-03/D-04/D-05).

Purpose: One search command with an `--engine` flag replaces three overlapping entry points.
Output: `websearch.go` deleted, `search_engines.go` deleted, `search.go` expanded with `--fetch` and all engines.
</objective>

<execution_context>
@/Users/dyamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dyamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-consolidation/03-CONTEXT.md
</context>

<interfaces>
<!-- From cmd/scout/search.go — existing structure -->
<!-- search <query> with --engine flag already present -->
<!-- search google/bing/duckduckgo/wikipedia subcommands from search_engines.go -->

<!-- From cmd/scout/websearch.go — capabilities to migrate into search.go -->
<!-- websearch <query>: same engine selection + optional --fetch flag that fetches each result page -->
<!-- --fetch flag triggers fetching result page content -->
<!-- --engine flag: google, bing, ddg -->

<!-- Decision D-03: merge websearch INTO search (search is the better name) -->
<!-- Decision D-04: remove websearch entirely -->
<!-- Decision D-05: remove search_engines subcommands (google/bing/duckduckgo) — redundant with --engine -->
<!-- Keep wikipedia subcommand as it is a unique engine not covered by --engine -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Merge websearch --fetch capability into search.go, delete websearch.go and search_engines.go</name>
  <files>cmd/scout/search.go, cmd/scout/websearch.go, cmd/scout/search_engines.go, cmd/scout/scout.go</files>
  <read_first>cmd/scout/search.go, cmd/scout/websearch.go, cmd/scout/search_engines.go, cmd/scout/scout.go</read_first>
  <action>
    Per CLI-03, D-03, D-04, D-05:
    1. Read `websearch.go` fully to understand the `--fetch` behaviour and any unique engine logic.
    2. Read `search_engines.go` to confirm the google/bing/duckduckgo subcommands are pure wrappers around `--engine`.
    3. In `search.go`:
       a. Add `--fetch` flag (bool) to the root `searchCmd`. When set, after retrieving search results, fetch the content of each result URL and include it in output. This is the core additional capability from `websearch`.
       b. Remove the `google`, `bing`, `duckduckgo` subcommands registered from `search_engines.go` — they are redundant with `--engine=google/bing/ddg`. Keep `wikipedia` subcommand (unique engine, per D-05 research: "Keep search wikipedia as it's a unique engine").
       c. Ensure `--engine` flag on `searchCmd` supports at minimum: google, bing, ddg (duckduckgo). Document valid values in flag usage string.
    4. In `scout.go`, remove `rootCmd.AddCommand(websearchCmd)` registration.
    5. Delete `cmd/scout/websearch.go`.
    6. Delete `cmd/scout/search_engines.go`.
    7. Run `go build ./cmd/scout/` to confirm no compile errors.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/scout/websearch.go` does not exist
    - `cmd/scout/search_engines.go` does not exist
    - `go build ./cmd/scout/` exits 0
    - `grep -r "websearchCmd\|AddCommand.*websearch" cmd/scout/` returns no matches
    - `grep "\-\-fetch" cmd/scout/search.go` returns a match (flag present)
    - `grep "wikipedia" cmd/scout/search.go` returns a match (wikipedia kept)
  </acceptance_criteria>
  <done>Single `search` command with `--engine` and `--fetch`; websearch and engine subcommands gone; build passes</done>
</task>

</tasks>

<verification>
- `go build ./cmd/scout/` passes
- `scout websearch` produces "unknown command"
- `scout search --help` shows `--engine` and `--fetch` flags
- `scout search wikipedia` still works
- `scout search google` produces "unknown command" (removed)
</verification>

<success_criteria>
CLI-03 satisfied: one `scout search` command with `--engine` flag replaces the three overlapping search entry points.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-consolidation/03-02-SUMMARY.md`
</output>

---
phase: 03-cli-consolidation
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/engine/lib/cdp/utils.go
  - pkg/scout/mcp/server.go
autonomous: true
requirements: [CLEAN-07, MCP-01]
must_haves:
  truths:
    - "`go build ./internal/...` passes with no errors after the tls.Dialer change"
    - "`grep 'golang v1.15' internal/engine/lib/cdp/utils.go` returns no matches"
    - "`grep 'tls\\.Dialer' internal/engine/lib/cdp/utils.go` returns at least one match (real implementation present)"
    - "`grep '33 tools\\|33 tool' pkg/scout/mcp/server.go` returns no matches"
    - "`grep '18 tools\\|18 tool\\|browser automation tools' pkg/scout/mcp/server.go` returns a match"
  artifacts:
    - path: internal/engine/lib/cdp/utils.go
      provides: tls.Dialer implementation replacing the TODO workaround
    - path: pkg/scout/mcp/server.go
      provides: Updated description string with correct tool count
  key_links:
    - from: internal/engine/lib/cdp/utils.go
      to: tls.Dialer
      via: direct usage
      pattern: "tls\\.Dialer"
---

<objective>
Fix the stale Go 1.15 `tls.Dialer` TODO (CLEAN-07, D-12) and correct the MCP help text tool count (MCP-01, D-13).

Purpose: Remove confusing legacy comments from the rod fork; ensure AI agents get accurate MCP capability information.
Output: `tls.Dialer` used directly in `cdp/utils.go`; MCP description corrected; chromium TODOs annotated.
</objective>

<execution_context>
@/Users/dyamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dyamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-consolidation/03-CONTEXT.md
</context>

<interfaces>
<!-- From internal/engine/lib/cdp/utils.go line 17: -->
<!-- // TODO: replace it with tls.Dialer once golang v1.15 is widely used. -->
<!-- Current Go version is 1.21+; tls.Dialer has been stable since Go 1.15 -->
<!-- Replace the manual TLS dial workaround with tls.Dialer{}.DialContext() -->

<!-- From pkg/scout/mcp/server.go: -->
<!-- The server Implementation or description contains "33 tools" — must be updated to "18 tools" -->
<!-- Find the string via: grep "33\|tools" pkg/scout/mcp/server.go -->
<!-- After the tool count fix, verify against actual AddTool call count in server.go -->

<!-- Other TODOs to triage per D-12: -->
<!-- internal/engine/lib/launcher/launcher.go:90 — site-per-process puppeteer issue → annotate "upstream limitation" -->
<!-- internal/engine/lib/launcher/launcher.go:567 — kill timing → annotate "empirical timing, monitor in prod" -->
<!-- internal/engine/page_rod.go:343 — chromium navigate close → annotate "upstream limitation: chromium CDP" -->
<!-- internal/engine/input.go:427 — crbug.com/613219 → annotate "upstream limitation: chromium bug" -->
<!-- internal/engine/lib/launcher/manager.go:27 — "better way to handle this" → add brief explanation of current approach -->
<!-- internal/engine/lib/launcher/flags/flags.go:10 — auto-generate flags → annotate "deferred: low value" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace tls.Dialer TODO with actual tls.Dialer implementation</name>
  <files>internal/engine/lib/cdp/utils.go</files>
  <read_first>internal/engine/lib/cdp/utils.go</read_first>
  <action>
    Per CLEAN-07, D-12 (fix Go 1.15 tls.Dialer reference immediately):
    1. Read `internal/engine/lib/cdp/utils.go` fully to understand the current TLS dial implementation.
    2. Find the TODO at line 17: `// TODO: replace it with tls.Dialer once golang v1.15 is widely used.`
    3. The current code likely uses a manual `tls.Client(conn, cfg)` approach or `tls.DialWithDialer`. Replace with `tls.Dialer{Config: cfg}.DialContext(ctx, network, addr)` pattern. Use the appropriate `tls.Dialer` API for the context (it takes NetDialer and Config).
    4. Remove the TODO comment. Add a brief comment explaining the implementation if non-obvious.
    5. For other TODOs in internal/ (see interfaces section above): read each file and annotate the TODO comments with either:
       - "upstream limitation: [brief reason]" for Chromium/puppeteer bugs
       - "deferred: [reason]" for low-value items
       - Brief inline explanation for timing/empirical items
       This is documentation-only change for those files — no code changes needed unless trivially fixable.
    6. Run `go build ./internal/...` to verify.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./internal/... 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/...` exits 0
    - `grep "golang v1.15" internal/engine/lib/cdp/utils.go` returns no matches
    - `grep "tls\.Dialer" internal/engine/lib/cdp/utils.go` returns a match (new implementation)
  </acceptance_criteria>
  <done>tls.Dialer TODO resolved with real implementation; other stale TODOs annotated</done>
</task>

<task type="auto">
  <name>Task 2: Fix MCP server description tool count from 33 to 18</name>
  <files>pkg/scout/mcp/server.go</files>
  <read_first>pkg/scout/mcp/server.go</read_first>
  <action>
    Per MCP-01, D-13:
    1. Read `pkg/scout/mcp/server.go` in full.
    2. Find the string "33 tools" (or similar phrasing referencing the old tool count) in the server description, Implementation description, or any help/usage text.
    3. Count the actual `mcp.AddTool` / `addTracedTool` calls in the file to confirm the current count is 18.
    4. Update the description string to reference "18 tools" (or remove the count entirely if it would require ongoing maintenance — use phrasing like "browser automation tools" instead of a hardcoded count).
    5. If the count is referenced in multiple places (e.g., comments, Description field, README references in strings), update all occurrences in this file.
    6. Run `go build ./pkg/scout/mcp/` to confirm no compile errors.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./pkg/scout/mcp/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./pkg/scout/mcp/` exits 0
    - `grep "33 tools\|33 tool" pkg/scout/mcp/server.go` returns no matches
    - `grep "18 tools\|18 tool\|browser automation tools" pkg/scout/mcp/server.go` returns a match (updated description)
  </acceptance_criteria>
  <done>MCP server description reflects actual tool count (18), not stale count (33)</done>
</task>

</tasks>

<verification>
- `go build ./internal/...` and `go build ./pkg/...` pass
- `grep "golang v1.15" internal/engine/lib/cdp/utils.go` returns empty
- `grep "33 tools" pkg/scout/mcp/server.go` returns empty
</verification>

<success_criteria>
CLEAN-07 and MCP-01 satisfied: stale TODO fixed with real implementation, MCP description correct.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-consolidation/03-05-SUMMARY.md`
</output>

---
phase: 03-cli-consolidation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/scout/markdown.go
  - cmd/scout/extract.go
  - cmd/scout/llm.go
  - cmd/scout/scout.go
autonomous: true
requirements: [CLI-04, CLI-06]
must_haves:
  truths:
    - "`scout markdown` no longer appears in `scout --help` output"
    - "`scout table` and `scout meta` no longer appear as top-level commands"
    - "`scout extract table` works (moved under extract parent)"
    - "`scout extract meta` works (moved under extract parent)"
    - "`scout extract ai` works (moved from `extract-ai` top-level)"
    - "`scout fetch --mode=markdown` continues to work unchanged"
  artifacts:
    - path: cmd/scout/markdown.go
      provides: DELETED — file must not exist
    - path: cmd/scout/extract.go
      provides: extract parent command with table/meta subcommands
    - path: cmd/scout/llm.go
      provides: extract-ai logic moved under extract parent (or extract.go)
  key_links:
    - from: cmd/scout/extract.go
      to: cmd/scout/scout.go
      via: rootCmd.AddCommand(extractCmd)
      pattern: "AddCommand.*extract"
---

<objective>
Remove `markdown` command (CLI-04, D-09) and group `table`/`meta`/`extract-ai` under an `extract` parent (CLI-06, D-10).

Purpose: Reduce top-level command clutter; `fetch --mode=markdown` is the canonical markdown path.
Output: `markdown.go` deleted, `extract.go` becomes a parent with `table`/`meta` subcommands, `extract-ai` moves under `extract ai`.
</objective>

<execution_context>
@/Users/dyamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dyamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-consolidation/03-CONTEXT.md
</context>

<interfaces>
<!-- From cmd/scout/extract.go — current state -->
<!-- Registers `table` and `meta` as top-level commands via rootCmd.AddCommand -->
<!-- These are currently bare cobra.Commands, not subcommands of a parent -->

<!-- From cmd/scout/llm.go — current state -->
<!-- Registers `extract-ai` as a top-level command via rootCmd.AddCommand -->
<!-- extract-ai: LLM-based structured extraction from a URL -->

<!-- From cmd/scout/markdown.go — to be deleted -->
<!-- markdown --url <url>: produces markdown of page, identical to fetch --mode=markdown -->

<!-- Target structure after this plan: -->
<!-- scout extract table <url>   (was: scout table) -->
<!-- scout extract meta <url>    (was: scout meta) -->
<!-- scout extract ai <url>      (was: scout extract-ai) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete markdown.go and remove its rootCmd registration</name>
  <files>cmd/scout/markdown.go, cmd/scout/scout.go</files>
  <read_first>cmd/scout/markdown.go, cmd/scout/scout.go</read_first>
  <action>
    Per CLI-04, D-09:
    1. Read `markdown.go` to confirm it is a pure subset of `fetch --mode=markdown` (same output, different entry).
    2. Delete `cmd/scout/markdown.go`.
    3. In `scout.go`, remove the `rootCmd.AddCommand(markdownCmd)` call (or equivalent).
    4. No deprecated alias — breaking changes are acceptable per project decisions.
    5. Run `go build ./cmd/scout/` to confirm no compile errors.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/scout/markdown.go` does not exist
    - `go build ./cmd/scout/` exits 0
    - `grep -r "markdownCmd\|AddCommand.*markdown" cmd/scout/` returns no matches
  </acceptance_criteria>
  <done>`markdown.go` deleted, no compile errors</done>
</task>

<task type="auto">
  <name>Task 2: Create extract parent and move table/meta/extract-ai under it</name>
  <files>cmd/scout/extract.go, cmd/scout/llm.go, cmd/scout/scout.go</files>
  <read_first>cmd/scout/extract.go, cmd/scout/llm.go, cmd/scout/scout.go</read_first>
  <action>
    Per CLI-06, D-10:
    1. Read `extract.go` fully — it currently registers `table` and `meta` as top-level commands.
    2. Read `llm.go` to find the `extract-ai` command definition.
    3. Restructure `extract.go`:
       a. Create a parent `extractCmd` cobra.Command (Use: "extract", Short: "Extract structured data from web pages").
       b. Move `table` and `meta` to be subcommands of `extractCmd` (change their registration from `rootCmd.AddCommand` to `extractCmd.AddCommand`).
       c. Move the `extract-ai` command from `llm.go` into `extractCmd` as `extractCmd.AddCommand(extractAICmd)`. Rename the cobra Use field to "ai" (so it appears as `scout extract ai`). Keep the implementation unchanged — only restructure the command registration.
    4. In `scout.go`: remove `rootCmd.AddCommand(tableCmd)`, `rootCmd.AddCommand(metaCmd)`, `rootCmd.AddCommand(extractAICmd)` (top-level registrations), and add `rootCmd.AddCommand(extractCmd)`.
    5. In `llm.go`: remove the `rootCmd.AddCommand(extractAICmd)` call (it is now registered via extractCmd in extract.go).
    6. Run `go build ./cmd/scout/` to confirm no compile errors.
    Note: If `extract-ai` command is defined in llm.go, move the command variable reference to extract.go or export it and import — use whichever approach avoids circular imports (both are in `main` package so no import needed, just reference the var).
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/scout/` exits 0
    - `grep "extractCmd.AddCommand" cmd/scout/extract.go` returns matches for table and meta
    - `grep -r "rootCmd.AddCommand.*tableCmd\|rootCmd.AddCommand.*metaCmd\|rootCmd.AddCommand.*extractAI" cmd/scout/` returns no matches
    - `grep "rootCmd.AddCommand.*extractCmd" cmd/scout/scout.go` returns a match
  </acceptance_criteria>
  <done>`scout extract table`, `scout extract meta`, `scout extract ai` all work; top-level `table`/`meta`/`extract-ai` gone</done>
</task>

</tasks>

<verification>
- `go build ./cmd/scout/` passes
- `scout markdown` produces "unknown command"
- `scout table` produces "unknown command"
- `scout meta` produces "unknown command"
- `scout extract-ai` produces "unknown command"
- `scout extract --help` lists: table, meta, ai
</verification>

<success_criteria>
CLI-04 and CLI-06 satisfied: markdown removed, extract commands grouped under single parent.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-consolidation/03-03-SUMMARY.md`
</output>

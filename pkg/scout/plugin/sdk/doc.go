// Package sdk is the Go SDK for Scout plugin authors. NewServer plus
// RegisterMode, RegisterTool, RegisterCommand, OnEvent, and friends wire a
// plugin binary into Scout's JSON-RPC protocol.
package sdk

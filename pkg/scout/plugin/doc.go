// Package plugin implements Scout's subprocess plugin system: JSON-RPC 2.0
// over stdin/stdout, manifest discovery, lazy process launch, and capability
// proxies (mode, tool, command, sink, auth, resource, middleware, event).
package plugin

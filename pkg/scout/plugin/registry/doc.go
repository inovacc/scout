// Package registry implements the Scout plugin marketplace: GitHub-backed
// JSON index, name/tag search, and a lock file tracking installed versions
// with SHA256 checksums at ~/.scout/plugins/lock.json.
package registry

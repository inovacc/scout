---
phase: 03-cli-consolidation
plan: "04"
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - cmd/scout/interact.go
  - cmd/scout/inspect.go
  - cmd/scout/navigate.go
  - cmd/scout/network.go
  - cmd/scout/screenshot.go
  - cmd/scout/mcp_screenshot.go
  - cmd/scout/storage.go
  - cmd/scout/window.go
  - cmd/scout/grpc_group.go
  - cmd/scout/scout.go
autonomous: true
requirements: [CLI-05, CLI-07]
must_haves:
  truths:
    - "The 17 bare gRPC commands (click, type, title, url, etc.) are no longer top-level commands"
    - "`scout grpc click`, `scout grpc type`, `scout grpc title` etc. work correctly"
    - "`scout screenshot` (standalone, requires gRPC session) moves to `scout grpc screenshot`"
    - "`scout mcp screenshot` (standalone, takes URL) remains or becomes `scout screenshot --url`"
    - "`scout --help` output is significantly shorter (no gRPC command clutter at root)"
  artifacts:
    - path: cmd/scout/grpc_group.go
      provides: grpcCmd parent command definition
    - path: cmd/scout/interact.go
      provides: grpcCmd subcommands for click/type/select/hover/focus/clear/key
    - path: cmd/scout/inspect.go
      provides: grpcCmd subcommands for title/url/text/attr/eval/html
    - path: cmd/scout/navigate.go
      provides: grpcCmd subcommands for navigate/back/forward/reload
    - path: cmd/scout/network.go
      provides: grpcCmd subcommands for cookie/header/block
    - path: cmd/scout/screenshot.go
      provides: grpcCmd subcommand for screenshot/pdf
    - path: cmd/scout/scout.go
      provides: grpcCmd parent registered on rootCmd
  key_links:
    - from: cmd/scout/scout.go
      to: grpcCmd
      via: rootCmd.AddCommand(grpcCmd)
      pattern: "AddCommand.*grpc"
---

<objective>
Group all 17 gRPC daemon commands under `scout grpc` (CLI-05, D-01/D-02) and consolidate screenshot dedup (CLI-07, D-11).

Purpose: Clean up 17 root-level clutter commands into a logical `grpc` subgroup; clarify screenshot naming.
Output: New `grpcCmd` parent; all daemon commands move under it; screenshot dedup resolved.
</objective>

<execution_context>
@/Users/dyamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dyamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-consolidation/03-CONTEXT.md
</context>

<interfaces>
<!-- Commands to move under scout grpc (all require running gRPC daemon): -->
<!-- From interact.go: click, type, select, hover, focus, clear, key -->
<!-- From inspect.go: title, url, text, attr, eval, html -->
<!-- From navigate.go: navigate, back, forward, reload -->
<!-- From network.go: cookie (the parent + all subcommands are already grouped), header, block -->
<!-- From screenshot.go (gRPC): screenshot, pdf -->
<!-- From storage.go: storage (get/set/list/clear) -->
<!-- From window.go: window (get/min/max/full/restore) -->

<!-- Screenshot situation (CLI-07, D-11): -->
<!-- cmd/scout/screenshot.go: gRPC-based, no URL arg, uses current session page → moves to grpc screenshot -->
<!-- cmd/scout/mcp_screenshot.go: standalone, takes URL arg → becomes root `scout screenshot --url` or stays as mcp screenshot -->
<!-- Decision D-11: "one canonical version; if gRPC-based it moves under scout grpc screenshot; standalone stays at root" -->
<!-- Resolution: gRPC screenshot → scout grpc screenshot; standalone mcp screenshot → scout screenshot (root) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create grpcCmd parent and move all gRPC daemon commands under it</name>
  <files>cmd/scout/grpc_group.go, cmd/scout/interact.go, cmd/scout/inspect.go, cmd/scout/navigate.go, cmd/scout/network.go, cmd/scout/storage.go, cmd/scout/window.go, cmd/scout/scout.go</files>
  <read_first>cmd/scout/interact.go, cmd/scout/inspect.go, cmd/scout/navigate.go, cmd/scout/network.go, cmd/scout/storage.go, cmd/scout/window.go, cmd/scout/scout.go</read_first>
  <action>
    Per CLI-05, D-01, D-02:
    1. Create a new file `cmd/scout/grpc_group.go` with the `grpcCmd` parent:
       ```go
       var grpcCmd = &cobra.Command{
           Use:   "grpc",
           Short: "Control a running gRPC daemon session",
           Long:  "Commands that require a running scout gRPC daemon (scout server). All commands interact with the current session page.",
       }
       ```
    2. In each of the following files, change every `rootCmd.AddCommand(xCmd)` to `grpcCmd.AddCommand(xCmd)` for daemon commands:
       - `interact.go`: click, type, select, hover, focus, clear, key
       - `inspect.go`: title, url, text, attr, eval, html
       - `navigate.go`: navigate, back, forward, reload
       - `network.go`: cookie (the parent + all subcommands are already grouped), header, block
       - `storage.go`: storage parent command
       - `window.go`: window parent command
    3. In `scout.go`: add `rootCmd.AddCommand(grpcCmd)` and remove all individual `rootCmd.AddCommand` calls for the above commands.
    4. Verify the `window` and `storage` commands are already structured as parent+subcommand groups — if so, just move the parent registration; subcommands stay as-is.
    5. Run `go build ./cmd/scout/` to confirm no compile errors.
    No deprecated aliases — breaking changes are acceptable per D-02.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/scout/` exits 0
    - `grep "grpcCmd.AddCommand" cmd/scout/interact.go cmd/scout/inspect.go cmd/scout/navigate.go` all return matches
    - `grep "rootCmd.AddCommand.*grpcCmd" cmd/scout/scout.go` returns a match
    - `grep -r "rootCmd.AddCommand.*clickCmd\|rootCmd.AddCommand.*typeCmd\|rootCmd.AddCommand.*titleCmd" cmd/scout/` returns no matches
  </acceptance_criteria>
  <done>17 gRPC commands grouped under `scout grpc`; root help is significantly shorter</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate screenshot — gRPC version under grpc, standalone at root</name>
  <files>cmd/scout/screenshot.go, cmd/scout/mcp_screenshot.go, cmd/scout/scout.go</files>
  <read_first>cmd/scout/screenshot.go, cmd/scout/mcp_screenshot.go, cmd/scout/scout.go</read_first>
  <action>
    Per CLI-07, D-11:
    1. Read both screenshot files to understand the distinction:
       - `screenshot.go`: gRPC-based, no URL, uses current session page — this belongs under `scout grpc screenshot`
       - `mcp_screenshot.go`: standalone, takes a URL, launches own browser — this becomes root `scout screenshot`
    2. In `screenshot.go`:
       - Change the command registration from `rootCmd.AddCommand(screenshotCmd)` / `rootCmd.AddCommand(pdfCmd)` to `grpcCmd.AddCommand(screenshotCmd)` / `grpcCmd.AddCommand(pdfCmd)`. This moves gRPC screenshot/pdf under `scout grpc screenshot` and `scout grpc pdf`.
    3. In `mcp_screenshot.go`:
       - The `mcp screenshot` subcommand (under `mcpCmd`) is a standalone browser launcher. Promote it: add it to `rootCmd` as the new `scout screenshot` command.
       - OR: if it is cleanly extractable from the `mcp` parent, extract it. If it is tightly coupled (shares mcpCmd state), duplicate the registration as both `mcp screenshot` and root `screenshot` (the former for backwards compat within `mcp` group, the latter for new canonical path).
       - Use the simplest approach that compiles cleanly. No alias needed since breaking changes are OK.
    4. Run `go build ./cmd/scout/` to confirm no compile errors.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/scout/` exits 0
    - `grep "grpcCmd.AddCommand.*screenshotCmd\|grpcCmd.AddCommand.*pdfCmd" cmd/scout/screenshot.go` returns matches
    - Root `scout screenshot` exists (standalone, takes URL) either via mcp_screenshot registration or new root registration
  </acceptance_criteria>
  <done>Screenshot dedup resolved: gRPC version at `scout grpc screenshot`, standalone at `scout screenshot`</done>
</task>

</tasks>

<verification>
- `go build ./cmd/scout/` passes
- `scout click` produces "unknown command"
- `scout grpc click --help` works
- `scout grpc screenshot --help` works (gRPC daemon version)
- `scout screenshot --help` works (standalone URL version)
- `scout --help` is noticeably shorter
</verification>

<success_criteria>
CLI-05 and CLI-07 satisfied: 17 gRPC commands grouped under `scout grpc`, screenshot dedup resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-consolidation/03-04-SUMMARY.md`
</output>

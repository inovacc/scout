---
phase: 03-cli-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/scout/credentials.go
  - cmd/scout/auth.go
  - cmd/scout/scout.go
autonomous: true
requirements: [CLI-02]
must_haves:
  truths:
    - "`scout credentials` no longer appears in `scout --help` output"
    - "`scout auth capture` accepts a `--plaintext` flag for unencrypted output"
    - "`scout auth replay` works as a new subcommand (migrated from `credentials replay`)"
    - "`scout auth show` works as a new subcommand (migrated from `credentials show`)"
  artifacts:
    - path: cmd/scout/credentials.go
      provides: DELETED — file must not exist
    - path: cmd/scout/auth.go
      provides: Updated auth command with capture/replay/show/login/logout/providers/status
  key_links:
    - from: cmd/scout/auth.go
      to: cmd/scout/scout.go
      via: rootCmd.AddCommand(authCmd)
      pattern: "AddCommand.*auth"
---

<objective>
Merge `credentials` into `auth` (CLI-02, per D-06/D-07/D-08).

Note: CLI-01 (recipe.go deletion) was completed in Phase 1 — recipe.go no longer exists. Task 1 below is therefore a no-op confirmation step; the real work is CLI-02.

Purpose: Unify credential capture under a single command group.
Output: `credentials.go` deleted, `auth.go` expanded with `replay`, `show`, and `--plaintext` on `capture`.
</objective>

<execution_context>
@/Users/dyamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dyamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cli-consolidation/03-CONTEXT.md
</context>

<interfaces>
<!-- From cmd/scout/credentials.go — executor must migrate these into auth.go -->
<!-- credentials capture: opens browser at URL, captures cookies+localStorage+sessionStorage, saves as JSON -->
<!-- credentials replay: restores captured session from JSON file into a new browser session -->
<!-- credentials show: prints captured credentials from file -->
<!-- credentials --on-close flag: auto-capture on browser close -->

<!-- From cmd/scout/auth.go — existing subcommands to preserve -->
<!-- auth login, auth capture, auth status, auth logout, auth providers -->
<!-- auth capture already does AES-encrypted capture; add --plaintext flag for unencrypted JSON output -->

<!-- From cmd/scout/scout.go — registration pattern -->
<!-- rootCmd.AddCommand(credentialsCmd) ← DELETE this line -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm recipe.go is absent (CLI-01 already done in Phase 1)</name>
  <files>cmd/scout/scout.go</files>
  <read_first>cmd/scout/scout.go</read_first>
  <action>
    CLI-01 was completed in Phase 1. Verify:
    1. Confirm `cmd/scout/recipe.go` does not exist.
    2. Confirm `cmd/scout/scout.go` contains no `rootCmd.AddCommand(recipeCmd)` reference.
    3. If any stale reference is found (unlikely), remove it and run `go build ./cmd/scout/`.
    This is a no-op confirmation — proceed immediately to Task 2 if all clear.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <done>`recipe.go` absent and no recipeCmd references in scout.go</done>
</task>

<task type="auto">
  <name>Task 2: Merge credentials into auth and delete credentials.go</name>
  <files>cmd/scout/auth.go, cmd/scout/credentials.go, cmd/scout/scout.go</files>
  <read_first>cmd/scout/auth.go, cmd/scout/credentials.go, cmd/scout/scout.go</read_first>
  <action>
    Per CLI-02, D-06, D-07, D-08:
    1. Read `credentials.go` fully to understand `capture`, `replay`, and `show` implementations.
    2. In `auth.go`:
       a. Add `--plaintext` flag to the existing `auth capture` subcommand. When `--plaintext` is set, write output as unencrypted JSON (same format as `credentials capture` output) instead of the AES-encrypted session file. This satisfies D-06 (unified capture with `--format`/`--plaintext` flag).
       b. Add `auth replay` subcommand — migrated directly from `credentials replay`. Restores captured session (handles both plaintext JSON and encrypted formats, detecting by file header or trying JSON first).
       c. Add `auth show` subcommand — migrated from `credentials show`. Prints credential file contents.
       d. If `credentials` also has an `--on-close` flag on capture, add it to `auth capture` as well.
    3. In `scout.go`, remove `rootCmd.AddCommand(credentialsCmd)`.
    4. Delete `cmd/scout/credentials.go`.
    5. Run `go build ./cmd/scout/` to confirm no compile errors.
    No deprecated alias for `credentials` — breaking changes are acceptable per D-02.
  </action>
  <verify>
    <automated>cd "C:/Users/dyamm/My Drive/acer/projects/scout" && go build ./cmd/scout/ 2>&1 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/scout/credentials.go` does not exist on disk
    - `go build ./cmd/scout/` exits 0
    - `grep -r "credentialsCmd\|AddCommand.*credentials" cmd/scout/` returns no matches
    - `grep "auth replay\|auth show\|plaintext" cmd/scout/auth.go` returns matches (new subcommands present)
  </acceptance_criteria>
  <done>`credentials.go` deleted, `auth` has `replay`/`show` subcommands and `--plaintext` on `capture`, build passes</done>
</task>

</tasks>

<verification>
After both tasks:
- `go build ./cmd/scout/` passes
- `scout credentials` produces "unknown command" (not in help)
- `scout auth --help` lists: login, capture, replay, show, logout, status, providers
- `scout auth capture --help` shows `--plaintext` flag
</verification>

<success_criteria>
CLI-02 satisfied: credentials command is gone, auth is the single credential capture surface.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-consolidation/03-01-SUMMARY.md`
</output>

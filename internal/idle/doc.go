package idle

// This file documents the idle package's role: it implements an idle-timeout
// timer used by the MCP and agent servers to auto-shutdown the browser after a
// period of inactivity. The canonical package doc comment lives in timer.go.
